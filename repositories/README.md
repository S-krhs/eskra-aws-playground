# Repositories

複数 app から参照する静的データと、そのデータを読み込む repository を置く領域です。

## 方針

- 実行ロジック、parser、外部 API client は置かない。
- app 固有の一時設定ではなく、共有して管理する価値があるデータだけを置く。
- 将来 DB や外部ストレージへ移す可能性があるデータは、README に用途と移行理由を残す。
