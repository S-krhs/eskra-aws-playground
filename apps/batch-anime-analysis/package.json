{
	"name": "@lambda-batch-playground/batch-anime-analysis",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"clean": "rm -rf dist",
		"build": "npm run clean && tsc -p tsconfig.json",
		"local": "node --loader ts-node/esm src/local-runner.ts",
		"test": "vitest run",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"lint": "biome ci src docs README.md package.json tsconfig.json",
		"validate": "npm run typecheck && npm run lint && npm run test"
	},
	"dependencies": {
		"@lambda-batch-playground/integration-discord": "0.1.0",
		"@lambda-batch-playground/libs-browser": "0.1.0",
		"@lambda-batch-playground/repositories": "0.1.0",
		"cheerio": "^1.1.2",
		"dotenv": "^17.4.2"
	},
	"devDependencies": {
		"ts-node": "^10.9.2"
	}
}
